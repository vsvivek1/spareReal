# Standing instructions

- After making code changes, always commit and push to `origin/main` without asking for confirmation first. This repo is linked to Vercel's Git integration, so a push auto-triggers a production deploy — that's expected and desired.
